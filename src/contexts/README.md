# Contexts Directory

React Context providers for global state.

## Contexts

- **AuthContext**: User authentication state
- **ThemeContext**: Dark/light theme (future)
- **NotificationContext**: Toast notifications

## Guidelines

1. Keep contexts focused
2. Provide TypeScript types
3. Use custom hooks for accessing context
4. Avoid deeply nested providers
5. Consider React Query for server state instead of Context
